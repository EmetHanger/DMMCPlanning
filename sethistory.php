<?php

    include_once 'modules/ModTracker/ModTracker.php';
            ModTracker::enableTrackingForModule(56);// obtain tabid of the module from vtiger_tab table
            ModTracker::enableTrackingForModule(57);// obtain tabid of the module from vtiger_tab table
            ModTracker::enableTrackingForModule(58);// obtain tabid of the module from vtiger_tab table
            ModTracker::enableTrackingForModule(59);// obtain tabid of the module from vtiger_tab table
            ModTracker::enableTrackingForModule(60);// obtain tabid of the module from vtiger_tab table
            ModTracker::enableTrackingForModule(61);// obtain tabid of the module from vtiger_tab table

